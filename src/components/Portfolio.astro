---
import projectList from "../../content/projectList.json";
import ProjectCard from "./ProjectCard/ProjectCard";
---

<section id="portfolio" class="container px-6 py-8 max-w-[430px] sm:max-w-[560px] lg:max-w-[900px] xl:max-w-[1300px] m-auto">
  <p class="w-fit text-2xl xl:w-2/3 font-worksans bg-clip-text text-transparent bg-gradient-to-r from-white to-custom-gray">Take a look to some of my projects.</p>
  <div id="projectList" class="flex flex-wrap gap-y-8 gap-x-6 mt-9 justify-center">
    {
      projectList.map((project, id) => {
        return <ProjectCard client:visible picture={project.picture} name={project.name} description={project.description} links={project.links} technologies={project.technologies} />;
      })
    }
  </div>
</section>
<style>
  #portfolio {
    min-height: 100vh;
  }
</style>
